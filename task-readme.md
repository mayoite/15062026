You MUST:
- Read AGENTS.md and then Readme.md before anything else.
- Respect all rules in AGENTS.md (scope, model routing, no tests without permission, minimal changes).
- Use a fast/mini model for scans and boilerplate; reserve deep/pro models for reasoning.

Task:
- [Describe the change very narrowly: e.g. “Clean up the secondary toolbar layout in features/planner/, improving grouping and responsiveness without changing behavior.”]

Process:
1. Summarize your understanding of the current implementation (files, components, data flow).
2. Propose a small, verifiable plan (list of specific files and edits).
3. Show the final diff as patches or code blocks, limited to the listed files.

Constraints:
- Do not touch files outside the scope unless I explicitly extend it.
- Do not run Playwright or any tests unless I say so.
- If you are unsure, stop and ask instead of guessing.
- Finish with a short “Done / Verified / Risks / Next” report as defined in AGENTS.md.