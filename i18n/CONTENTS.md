# i18n

## Why this folder exists

Locale routing plus locale message loading for next-intl.

## What is here

- config.ts — locale list and default locale
- routing.ts — next-intl route config
- request.ts — locale resolver and message loader
- messages/ — locale JSON files used by the app and tests

