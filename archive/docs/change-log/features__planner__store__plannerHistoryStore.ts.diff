diff --git a/features/planner/store/plannerHistoryStore.ts b/features/planner/store/plannerHistoryStore.ts
index 02dedfe..ca8ce94 100644
--- a/features/planner/store/plannerHistoryStore.ts
+++ b/features/planner/store/plannerHistoryStore.ts
@@ -33,20 +33,20 @@ export const usePlannerHistoryStore = create<HistoryState>((set, get) => ({
 
   undo: (currentSnapshot) => {
     const s = get();
-    const nextState = buildUndoState(s, currentSnapshot);
-    if (nextState) {
-      set({ undoStack: nextState.undoStack, redoStack: nextState.redoStack });
-      return nextState;
+    const result = buildUndoState(s, currentSnapshot);
+    if (result) {
+      set({ undoStack: result.undoStack, redoStack: result.redoStack });
+      return result.snapshot;
     }
     return null;
   },
 
   redo: (currentSnapshot) => {
     const s = get();
-    const nextState = buildRedoState(s, currentSnapshot);
-    if (nextState) {
-      set({ undoStack: nextState.undoStack, redoStack: nextState.redoStack });
-      return nextState;
+    const result = buildRedoState(s, currentSnapshot);
+    if (result) {
+      set({ undoStack: result.undoStack, redoStack: result.redoStack });
+      return result.snapshot;
     }
     return null;
   },
