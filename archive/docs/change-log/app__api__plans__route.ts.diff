diff --git a/app/api/plans/route.ts b/app/api/plans/route.ts
index 519d698..3aa2f71 100644
--- a/app/api/plans/route.ts
+++ b/app/api/plans/route.ts
@@ -1,6 +1,7 @@
 import type { NextRequest} from "next/server";
 import { NextResponse } from "next/server";
 import { createServerClient } from "@/lib/supabase/server";
+import { listPlannerDocumentsFromStore } from "@/features/planner/store/plannerSaves";
 
 type PublishBody = {
   id?: string;
@@ -38,6 +39,31 @@ function computeItemCount(data: Record<string, unknown>): number {
   return furniture + rooms + walls + doors + windows;
 }
 
+export async function GET() {
+  const supabase = await createServerClient();
+  const { data: authData } = await supabase.auth.getUser();
+  const userId = authData.user?.id ?? null;
+
+  if (!userId) {
+    return NextResponse.json(
+      { error: "Authentication required" },
+      { status: 401 },
+    );
+  }
+
+  try {
+    const documents = await listPlannerDocumentsFromStore({ userId });
+    return NextResponse.json({ documents });
+  } catch (error) {
+    return NextResponse.json(
+      {
+        error: `Failed to list plans: ${error instanceof Error ? error.message : String(error)}`,
+      },
+      { status: 500 },
+    );
+  }
+}
+
 export async function POST(req: NextRequest) {
   const body = (await req.json().catch(() => ({}))) as PublishBody;
   const id = typeof body.id === "string" ? body.id.trim() : "";
