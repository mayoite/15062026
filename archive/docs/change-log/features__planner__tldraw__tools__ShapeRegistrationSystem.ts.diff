diff --git a/features/planner/tldraw/tools/ShapeRegistrationSystem.ts b/features/planner/tldraw/tools/ShapeRegistrationSystem.ts
index f5a2333..b4f0cf5 100644
--- a/features/planner/tldraw/tools/ShapeRegistrationSystem.ts
+++ b/features/planner/tldraw/tools/ShapeRegistrationSystem.ts
@@ -5,7 +5,7 @@
  * shapes against their schemas, and managing shape lifecycle.
  */
 
-import type { Editor, TLShape, TLShapePartial } from "@tldraw/editor";
+import type { Editor, TLShape, TLShapePartial, TLShapeId } from "@tldraw/editor";
 import {
   getValidationErrors,
   safeValidateDoorShape,
@@ -443,7 +443,7 @@ export class ShapeRegistrationSystem {
 
   // Update shape with validation
   updateValidatedShape(shapeId: string, updates: Partial<TLShape>): boolean {
-    const shape = this.editor.getShape(shapeId as unknown);
+    const shape = this.editor.getShape(shapeId as TLShapeId);
     if (!shape) return false;
 
     const updatedShape = { ...shape, ...updates } as TLShape;
@@ -467,7 +467,7 @@ export class ShapeRegistrationSystem {
   // Delete shape with cleanup
   deleteShapeWithCleanup(shapeId: string): boolean {
     try {
-      this.editor.deleteShape(shapeId as unknown);
+      this.editor.deleteShape(shapeId as TLShapeId);
       this.validationCache.delete(shapeId);
       return true;
     } catch (error) {
@@ -501,7 +501,7 @@ export class ShapeRegistrationSystem {
           };
 
           try {
-            this.editor.updateShape(fixedShape as unknown);
+            this.editor.updateShape(fixedShape as TLShapePartial<TLShape>);
             this.validationCache.delete(shape.id);
             fixed++;
 // eslint-disable-next-line @typescript-eslint/no-unused-vars
