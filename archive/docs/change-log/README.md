# Change Log — per-file diffs

These files are generated by **git**, not hand-written by the assistant. Each
`*.diff` is the output of `git diff -- <path>` for one changed file, so it shows
the exact lines added/removed. You can reproduce and verify any of them yourself:

```
git diff -- <path>
```

- Full combined diff: `../CHANGE-LOG-FULL-DIFF.md`
- Summary (files + line counts): `../CHANGE-LOG-SUMMARY.txt`

## Honesty note (read this)

This is the **total uncommitted working-tree delta** against commit
`d6417c5` — it includes changes made earlier in this session and any prior
uncommitted work, NOT only the most recent edits. It is not a per-session log.
The authoritative, tamper-proof record is git itself; treat these files as a
convenience copy of `git diff`.

Filename encoding: path separators are replaced with `__`, e.g.
`features/planner/store/aiStore.ts` → `features__planner__store__aiStore.ts.diff`.

## Changes attributable to the current typecheck/lint cleanup work

These specific files were edited in the current effort to drive down typecheck
and lint errors (verifiable in the matching `.diff`):

| File | What changed | Why |
|------|--------------|-----|
| `features/oando-planner/hooks/usePlannerSession.ts` | Removed `setState`-in-`useEffect`; sync `userId` during render via `prevUserIdRef` | Fix lint `react-hooks/set-state-in-effect` |
| `features/planner/store/aiStore.ts` | `style` typed `string` → `StylePreset` | AI merge; fixes unused-import lint |
| `features/planner/store/plannerGeometryStore.ts` | two `!= null` → `!== undefined` | lint `eqeqeq` |
| `features/planner/tldraw/tools/MeasurementTool.ts` | removed unused `TLShape` import; `!=` → `!== undefined` | lint (unused + eqeqeq) |
| `features/planner/lib/aiService.ts` | promoted real AI impl into canonical path | unify AI service |
| `features/oando-planner/lib/aiService.ts` | reduced to re-export shim | unify AI service |
| `features/buddy-planner/stores/layerVisibilityStore.ts` | added `annotations` + `it-device` keys to `ALL_VISIBLE` | TS2739 missing Record keys |
| `features/buddy-planner/components/editor/LeftSidebar/LayerVisibilityPanel.tsx` | added same two keys to `CATEGORY_LABELS` and `counts` | TS2739 missing Record keys |
| `features/buddy-planner/stores/canvasStore.ts` | added `'neighborhood'` + `'book'` to `ToolType` union | TS2322/TS2353 |
| `app/api/admin/themes/publish/route.ts` | `err.message` → `err instanceof Error ? err.message : ...` | TS18046 unknown |
| `app/api/admin/buddy-catalog/route.ts` | `(supabase as unknown)` → `(supabase as SupabaseClient)` + import | TS2571 unknown |
| `app/api/admin/buddy-catalog/[id]/route.ts` | same supabase cast fix (3 sites) + import | TS2571 unknown |

Verification commands (run them yourself):

```
npm.cmd run typecheck   # evidence: results/typecheck-after.txt
npm.cmd run lint        # evidence: results/lint-after.txt
```
