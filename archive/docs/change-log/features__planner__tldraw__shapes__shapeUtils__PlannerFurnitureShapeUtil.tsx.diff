diff --git a/features/planner/tldraw/shapes/shapeUtils/PlannerFurnitureShapeUtil.tsx b/features/planner/tldraw/shapes/shapeUtils/PlannerFurnitureShapeUtil.tsx
index b243884..b79ba1b 100644
--- a/features/planner/tldraw/shapes/shapeUtils/PlannerFurnitureShapeUtil.tsx
+++ b/features/planner/tldraw/shapes/shapeUtils/PlannerFurnitureShapeUtil.tsx
@@ -101,4 +101,11 @@ export class PlannerFurnitureShapeUtil extends ShapeUtil<PlannerFurnitureTLShape
     const { w, h } = footprintCanvas(shape);
     return <rect width={w} height={h} rx={4} />;
   }
+
+  getIndicatorPath(shape: PlannerFurnitureTLShape) {
+    const { w, h } = footprintCanvas(shape);
+    const path = new Path2D();
+    path.roundRect(0, 0, w, h, 4);
+    return path;
+  }
 }
