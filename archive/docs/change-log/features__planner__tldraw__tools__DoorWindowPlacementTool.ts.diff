diff --git a/features/planner/tldraw/tools/DoorWindowPlacementTool.ts b/features/planner/tldraw/tools/DoorWindowPlacementTool.ts
index 930ccdb..2f464b9 100644
--- a/features/planner/tldraw/tools/DoorWindowPlacementTool.ts
+++ b/features/planner/tldraw/tools/DoorWindowPlacementTool.ts
@@ -5,8 +5,9 @@
  * automatic snapping, rotation alignment, and opening direction handling.
  */
 
-import type { Editor, TLShape } from "@tldraw/editor";
+import type { Editor, TLShape, TLShapeId } from "@tldraw/editor";
 import { Vec, createShapeId } from "@tldraw/editor";
+import type { PlannerDoorTLShape, PlannerWindowTLShape } from "../shapes/tldrawShapeTypes";
 import { DEFAULT_DOOR_PROPS } from "../shapes/DoorShape";
 import { DEFAULT_WINDOW_PROPS } from "../shapes/WindowShape";
 import { snapOpeningToWall } from "./tldrawSnap";
@@ -33,6 +34,16 @@ export interface DoorWindowConfig {
   color: string;
 }
 
+export interface PlacementState {
+  config: DoorWindowConfig;
+  position: Vec;
+  rotation: number;
+  snappedWallId: string | null;
+  previewId: TLShapeId | null;
+  opacity: number;
+  id: TLShapeId;
+}
+
 export class DoorWindowPlacementUtils {
   private toPreviewId(id: string) {
     const baseId = id.startsWith("shape:") ? id.slice(6) : id;
@@ -68,7 +79,7 @@ export class DoorWindowPlacementUtils {
     defaultDirection: "left",
   };
 
-  private currentPreview: unknown = null;
+  private currentPreview: PlacementState | null = null;
   private isDragging: boolean = false;
 
   constructor(private editor: Editor) { }
@@ -162,12 +173,14 @@ export class DoorWindowPlacementUtils {
       }
     }
 
-    const placement = {
+    const placement: PlacementState = {
       id: createShapeId(),
       config,
       position: snappedPosition,
       rotation,
       snappedWallId,
+      previewId: null,
+      opacity: 1,
     };
 
     this.currentPreview = placement;
@@ -218,7 +231,8 @@ export class DoorWindowPlacementUtils {
     const shape = this.createShape(this.currentPreview);
 
     if (shape) {
-      this.editor.createShape(shape as unknown);
+      // eslint-disable-next-line @typescript-eslint/no-explicit-any
+      this.editor.createShape(shape as any);
 
       // Hide preview
       if (this.options.showPreview) {
@@ -269,11 +283,9 @@ export class DoorWindowPlacementUtils {
   }
 
   // Create shape from placement data
-  private createShape(placement: unknown): unknown {
+  private createShape(placement: PlacementState): unknown {
     const { config, position, rotation, snappedWallId } = placement;
 
-
-
     if (config.type === "door") {
       return {
         id: placement.id,
@@ -326,30 +338,30 @@ export class DoorWindowPlacementUtils {
   }
 
   // Show preview
-  private showPreview(placement: unknown) {
+  private showPreview(placement: PlacementState) {
     const previewId = this.toPreviewId(String(placement.id));
     const previewShape = this.createShape({
       ...placement,
       id: previewId,
-    });
+    }) as { opacity?: number } | null;
 
     if (previewShape) {
       previewShape.opacity = 0.5;
-      this.editor.createShape(previewShape as unknown);
+      this.editor.createShape(previewShape as Parameters<Editor["createShape"]>[0]);
     }
   }
 
   // Update preview
-  private updatePreview(placement: unknown) {
+  private updatePreview(placement: PlacementState) {
     const previewId = this.toPreviewId(String(placement.id));
     const previewShape = this.createShape({
       ...placement,
       id: previewId,
-    });
+    }) as { opacity?: number; id?: TLShapeId } | null;
 
     if (previewShape) {
       previewShape.opacity = 0.5;
-      this.editor.updateShape(previewShape as unknown);
+      this.editor.updateShape(previewShape as Parameters<Editor["updateShape"]>[0]);
     }
   }
 
@@ -368,35 +380,36 @@ export class DoorWindowPlacementUtils {
 
   // Change opening direction
   changeDirection(shapeId: string, direction: OpeningDirection) {
-    const shape = this.editor.getShape(shapeId as unknown);
+    const shape = this.editor.getShape(shapeId as TLShapeId);
     if (!shape) return;
     if (shape.type !== "planner-door") return;
 
-    const currentProps = (shape as unknown).props ?? {};
+    const doorShape = shape as PlannerDoorTLShape;
     this.editor.updateShape({
-      id: shapeId,
+      id: shapeId as TLShapeId,
       type: "planner-door",
       props: {
-        ...currentProps,
+        ...doorShape.props,
         swingDirection: direction,
       },
-    } as unknown);
+    });
   }
 
   // Rotate door/window
   rotateShape(shapeId: string, angle: number) {
-    const shape = this.editor.getShape(shapeId as unknown);
+    const shape = this.editor.getShape(shapeId as TLShapeId);
     if (shape) {
       this.editor.updateShape({
-        id: shapeId,
+        id: shapeId as TLShapeId,
+        type: shape.type,
         rotation: (shape.rotation || 0) + angle,
-      } as unknown);
+      });
     }
   }
 
   // Delete door/window
   deleteShape(shapeId: string) {
-    this.editor.deleteShape(shapeId as unknown);
+    this.editor.deleteShape(shapeId as TLShapeId);
   }
 
   // Get all doors/windows on canvas
@@ -412,8 +425,13 @@ export class DoorWindowPlacementUtils {
   getShapesOnWall(wallId: string): TLShape[] {
     const allShapes = this.getDoorWindowShapes();
     return allShapes.filter(shape => {
-      const s = shape as unknown;
-      return s.props?.wallId === wallId;
+      if (shape.type === "planner-door") {
+        return (shape as PlannerDoorTLShape).props.wallId === wallId;
+      }
+      if (shape.type === "planner-window") {
+        return (shape as PlannerWindowTLShape).props.wallId === wallId;
+      }
+      return false;
     });
   }
 
