diff --git a/features/planner/store/plannerHistoryUtils.ts b/features/planner/store/plannerHistoryUtils.ts
index 391c326..5d6aaf0 100644
--- a/features/planner/store/plannerHistoryUtils.ts
+++ b/features/planner/store/plannerHistoryUtils.ts
@@ -1,34 +1,38 @@
-import type { PlannerState } from "./plannerStore";
 import type { HistorySnapshot } from "./plannerStoreSupport";
 
+interface HistoryStacks {
+  undoStack: HistorySnapshot[];
+  redoStack: HistorySnapshot[];
+}
+
+export interface UndoRedoResult extends HistoryStacks {
+  snapshot: HistorySnapshot;
+}
+
 export function buildUndoState(
-  state: PlannerState,
+  state: HistoryStacks,
   currentSnapshot: HistorySnapshot
-): Partial<PlannerState> | null {
+): UndoRedoResult | null {
   if (state.undoStack.length === 0) return null;
 
   const previous = state.undoStack[state.undoStack.length - 1];
   return {
-    ...previous,
+    snapshot: previous,
     undoStack: state.undoStack.slice(0, -1),
     redoStack: [...state.redoStack, currentSnapshot],
-    selectedId: null,
-    isDirty: true,
   };
 }
 
 export function buildRedoState(
-  state: PlannerState,
+  state: HistoryStacks,
   currentSnapshot: HistorySnapshot
-): Partial<PlannerState> | null {
+): UndoRedoResult | null {
   if (state.redoStack.length === 0) return null;
 
   const next = state.redoStack[state.redoStack.length - 1];
   return {
-    ...next,
+    snapshot: next,
     redoStack: state.redoStack.slice(0, -1),
     undoStack: [...state.undoStack, currentSnapshot],
-    selectedId: null,
-    isDirty: true,
   };
 }
