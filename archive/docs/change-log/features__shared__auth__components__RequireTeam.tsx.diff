diff --git a/features/shared/auth/components/RequireTeam.tsx b/features/shared/auth/components/RequireTeam.tsx
deleted file mode 100644
index 7ae0c6f..0000000
--- a/features/shared/auth/components/RequireTeam.tsx
+++ /dev/null
@@ -1,17 +0,0 @@
-import { redirect } from 'next/navigation'
-import { useMyTeams } from '@/lib/teams/useMyTeams'
-import { RouteLoadingFallback } from '@/features/shared/ui/RouteLoadingFallback'
-
-export function RequireTeam({ children }: { children: React.ReactNode }) {
-  const teams = useMyTeams()
-  if (teams === null) {
-    return <RouteLoadingFallback />
-  }
-  if (teams.length === 0) {
-    redirect("/onboarding/team")
-  }
-  return <>{children}</>
-}
-
-
-
