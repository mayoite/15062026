diff --git a/features/planner/editor/plannerCamera.ts b/features/planner/editor/plannerCamera.ts
index 305b3b4..0959791 100644
--- a/features/planner/editor/plannerCamera.ts
+++ b/features/planner/editor/plannerCamera.ts
@@ -10,8 +10,9 @@ export const PLANNER_DEFAULT_FLOOR_BOUNDS = {
 
 const FIT_PADDING = 140;
 
-export function configurePlannerCamera(editor: Editor) {
-  editor.setCameraOptions({ zoomToFitPadding: FIT_PADDING });
+export function configurePlannerCamera(_editor: Editor) {
+  // Camera padding is set via inset parameter on zoomToBounds/zoomToFit calls
+  // tldraw 5.x removed zoomToFitPadding from camera options
 }
 
 /** Zoom out so users see a full floor slab before placing furniture. */
