diff --git a/features/planner/tldraw/tools/PlannerMeasurementTool.ts b/features/planner/tldraw/tools/PlannerMeasurementTool.ts
index f831626..0a42ccb 100644
--- a/features/planner/tldraw/tools/PlannerMeasurementTool.ts
+++ b/features/planner/tldraw/tools/PlannerMeasurementTool.ts
@@ -7,6 +7,7 @@ import {
   type TLStateNodeConstructor,
 } from "@tldraw/editor";
 import { SnapManager } from "@/features/planner/lib/snapManager";
+import type { PlannerMeasurementTLShape } from "../shapes/tldrawShapeTypes";
 
 /**
  * PlannerMeasurementTool
@@ -171,8 +172,9 @@ class PlannerMeasurementToolDrawing extends StateNode {
     if (this.measureId && this.startPoint) {
       const shape = this.editor.getShape(this.measureId);
       if (shape && shape.type === "planner-measurement") {
-        const dx = (shape.props as unknown).endX;
-        const dy = (shape.props as unknown).endY;
+        const measureShape = shape as PlannerMeasurementTLShape;
+        const dx = measureShape.props.endX;
+        const dy = measureShape.props.endY;
         if (Math.hypot(dx, dy) < 4) {
           this.editor.deleteShape(this.measureId);
         }
