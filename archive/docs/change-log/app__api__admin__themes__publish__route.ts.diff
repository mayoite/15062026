diff --git a/app/api/admin/themes/publish/route.ts b/app/api/admin/themes/publish/route.ts
index b6c2572..0500a44 100644
--- a/app/api/admin/themes/publish/route.ts
+++ b/app/api/admin/themes/publish/route.ts
@@ -96,6 +96,8 @@ export async function POST(req: Request) {
     });
   } catch (err: unknown) {
     console.error("CDN Upload Error:", err);
-    return NextResponse.json({ success: false, error: err.message }, { status: 500 });
+    const message = err instanceof Error ? err.message : "CDN upload failed";
+    return NextResponse.json({ success: false, error: message }, { status: 500 });
   }
+
 }
