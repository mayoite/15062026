diff --git a/features/planner/tldraw/tools/WallTool.ts b/features/planner/tldraw/tools/WallTool.ts
index 600ba50..8171ab6 100644
--- a/features/planner/tldraw/tools/WallTool.ts
+++ b/features/planner/tldraw/tools/WallTool.ts
@@ -274,8 +274,9 @@ class PlannerWallToolDrawing extends StateNode {
 // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
     const shape = this.editor.getShape(this.wallId!);
     if (shape && shape.type === "planner-wall") {
-      const dx = (shape.props as unknown).endX;
-      const dy = (shape.props as unknown).endY;
+      const wallShape = shape as PlannerWallTLShape;
+      const dx = wallShape.props.endX;
+      const dy = wallShape.props.endY;
       const length = Math.hypot(dx, dy);
 
       if (length < 4) {
@@ -329,8 +330,9 @@ class PlannerWallToolDrawing extends StateNode {
 
     for (const shape of allShapes) {
       if (shape.type === "planner-wall" && shape.id !== excludeId) {
-        const start = new Vec(shape.x + (shape.props as unknown).startX, shape.y + (shape.props as unknown).startY);
-        const end = new Vec(shape.x + (shape.props as unknown).endX, shape.y + (shape.props as unknown).endY);
+        const wallShape = shape as PlannerWallTLShape;
+        const start = new Vec(shape.x + wallShape.props.startX, shape.y + wallShape.props.startY);
+        const end = new Vec(shape.x + wallShape.props.endX, shape.y + wallShape.props.endY);
 
         if (point.dist(start) < threshold || point.dist(end) < threshold) {
           endpointHits++;
