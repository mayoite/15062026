# Archived CSS — 2026-06-14

Removed from global `app/css/index.css` during Phase 1–3 CSS migration.

| File | Reason |
|---|---|
| `hero-slider.css` | Zero class usage in live `.tsx` |
| `partners.css` | Replaced by `home-partnership-panel` in `routes/home/` |
| `service.css` | Zero class usage in live `.tsx` |
| `social.css` | Zero class usage in live `.tsx` |

Restore only if a route is reintroduced that uses these class names.