# API route handlers

## Why this folder exists

Server endpoints for catalog, planner saves, admin, AI, tracking — called from client or scripts.

## What is here

- admin/ — admin portal APIs
- planner/ — planner persistence & AI
- products/, categories/ — catalog
- ai/, ai-advisor/, ai-assist/ — AI proxies

## Rules

- Use platform/supabase or platform/drizzle — never raw credentials in handlers

---
*Generated by `scripts/generate-contents-md.mjs` — edit manifest, then `npm run docs:sync:all`.*
