"use client";
import React from 'react';
/** Stub — AdminLayoutShell not yet implemented. */
export default function AdminLayoutShell({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
