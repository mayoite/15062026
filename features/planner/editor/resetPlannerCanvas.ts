/** Fabric-era stub — canvas reset is handled by the fabric workspace. */
export function confirmResetPlannerCanvas(): boolean {
  return window.confirm("Reset the planner canvas? This cannot be undone.");
}