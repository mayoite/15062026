"use client";

/**
 * Floating canvas chrome (steps + panel access) moved to PlannerTopBar + PlannerSubTopBar.
 * Kept as a no-op host so workspace layout refs stay stable during the fabric replacement.
 */

export function PlannerChromeHost() {
  return null;
}